import React from 'react';

type Props = {
    iconUrl?: string,
    label?: string,
    options?: string[],
    placeholder?: string,
    onChange?: () => void,
}

function Select() {
    return (
        <div>
            
        </div>
    )
}

export default Select;
